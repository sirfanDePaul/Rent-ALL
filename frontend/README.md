RentAll frontend (React)

Quick start:
1. cd frontend
2. npm install
3. npm start

React frontend for RentAll. It uses the backend `/api` routes for authentication, listings, rental requests, messages, and reviews.
